<%= form_for @call_record do |f| %>
    <%= render :partial => 'call_records/form', :locals => { :f => f } %>

    <div class="form-group mb-0">
        <%= f.submit t('action.submit'), :class => 'btn btn-submit' %>
    </div>
<% end %>